server:
  port: 8080

spring:
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "https://coalbase.io, coalbase.io,  https://coalbase.archi-lab.io, archi-lab.io"
            allowedMethods:
              - GET
              - POST
              - PATCH
              - PUT
              - DELETE
              - OPTIONS
              - HEAD
      routes:
        - id: learning-outcomes
          uri: learning-outcome_learning_outcome_ms
          predicates:
            - Path=/learningOutcomes, /learningOutcomes/{segment}
        - id: learning-spaces
          uri: learning-outcome_learning_outcome_ms
          predicates:
            - Path=/learningSpaces, /learningSpaces/{segment}
        - id: courses
          uri: learning-outcome_learning_outcome_ms
          predicates:
            - Path=/courses, /courses/{segment}
